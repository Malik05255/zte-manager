# Honor 200 home UI reference

The active home dashboard uses native Compose layout without whole-screen scaling. Its geometry follows the approved Honor 200 reference: embedded radio metrics in the hero card, tall speed-test card on the left, network-mode and tower cards stacked on the right, bands and live-signal cards on the final row, and shared header/bottom navigation chrome across all sections.
